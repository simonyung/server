<%= simple_form_for @post do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.input :user_id, as: :hidden, input_html: { :value => current_user.id} %>
    <%= f.button :submit %>
<% end %>